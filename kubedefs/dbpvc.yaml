apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vprofile-storage-dbpvc
  labels:
    app: vprodb
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: default

    